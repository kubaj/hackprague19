import 'package:flutter/material.dart';

class RatingIndicator<T> extends StatelessWidget {
  RatingIndicator({this.value});

  final double value;

  @override
  Widget build(BuildContext context) {
    return new Row(
      children: <Widget>[
        SizedBox(

        ),

      ],
    );
  }

}